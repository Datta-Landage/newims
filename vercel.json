{
  "version": 2,
  "outputDirectory": "dist",
  "buildCommand": "npm run build",
  "devCommand": "tsx watch --env-file=.env src/index.ts",

  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}
